{
  "name": "claude-matrix-bridge",
  "version": "1.0.0",
  "description": "Bridge Matrix messages directly to Claude Code CLI sessions",
  "type": "module",
  "engines": {
    "node": ">=22.0.0"
  },
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "vitest run",
    "bootstrap-crosssigning": "node bootstrap-crosssigning.mjs",
    "lint": "eslint . --max-warnings=0",
    "check": "node --check index.js && node --check ask-user.js && node --check viewer/server.js && node --check viewer/start.js && node --check setup-user.mjs && node --check verify-bots.mjs && node --check bootstrap-crosssigning.mjs && node --check lib/transcribe.js && node --check lib/viewer-tokens.js && node --check lib/mcp-config-mac.js && node --check lib/mcp-config.js && node --check lib/pre-trust.js && node --check lib/pty-input.js && node --check lib/transcript-tail.js && node --check lib/prompt-detector.js && node --check lib/interactive-session.js && node --check lib/iv-uploads.js && node --check test/stub-claude.mjs",
    "audit:high": "npm audit --audit-level=high",
    "ci": "npm run lint && npm run check && npm test && npm run audit:high"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.102.0",
    "@google/generative-ai": "^0.24.1",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "matrix-bot-sdk": "^0.8.0",
    "matrix-js-sdk": "^41.6.0",
    "node-pty": "^1.0.0",
    "ws": "^8.21.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.4.1",
    "globals": "^17.6.0",
    "vitest": "^4.1.8"
  },
  "license": "AGPL-3.0-only",
  "overrides": {
    "form-data": "^2.5.5",
    "qs": "^6.14.1",
    "tough-cookie": "^4.1.4",
    "matrix-js-sdk": {
      "uuid": "^14.0.0"
    },
    "sanitize-html": "^2.17.4",
    "fast-uri": "^3.1.2"
  }
}
